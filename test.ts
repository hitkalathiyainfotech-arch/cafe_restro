type IEvent = {
  eventImage: String,
  eventName: String,
  eventDesc: String
}

const createEvent: IEvent = {
  eventName: "corate meatting",
  eventImage: "https://google.com",
  eventDesc: "swsd"
}

console.log(createEvent)